---
# Projects for EDA Controller
eda_projects:
  - name: "{{ project_name }}"
    description: "EDA Rulebooks for Dynatrace problem event handling"
    url: "{{ project_scm_url }}"
    scm_branch: "{{ project_scm_branch }}"
    credential: "GitHub PAT"
    organization: "{{ organization_name }}"
